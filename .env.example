# URL of the ClickUp MCP server (Streamable HTTP transport)
VITE_CLICKUP_MCP_URL=https://mcp.clickup.com/mcp

# Optional: REST fallback base URL, used only if MCP tool calls fail
VITE_CLICKUP_REST_URL=https://api.clickup.com/api/v2
